info:
  name: GetGrados
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:5235/api
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
